# Questionairre-Python
A simple Python program that randomly selects a user-defined number of questions from a predefined question bank. It uses random.sample() to ensure unique selection and includes input validation to prevent invalid or out-of-range requests. 
